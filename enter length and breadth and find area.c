#include<stdio.h>
int main()
{
	float length,breadth,area;
	scanf("%f%f",&length,&breadth);
	area=length*breadth; // area of rectangle;
	printf("%f,%f\n",length,breadth);
	printf("%f",area);
	return 0;
}
